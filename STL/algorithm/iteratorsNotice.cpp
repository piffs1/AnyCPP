#include <iostream>


using namespace std;

namespace STL_BOOK {

	/// BiderectionalIterator -> Могут быть ++it и --it, но не могут быть смещены на определенное растояние в отличии
	/// от RandomAccessIterator. К примеру it - 5 недопустимо для BiderectionalIterator в отличии от RandomAccessIterator
	/// ForwardIterator Однонаправленные или прямые итераторы
	/// InputIterator
	/// OutpurIterator
}
int main()
{
    return 0;
}
